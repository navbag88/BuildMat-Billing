<configuration>

    <!--
        Log file path is configurable via the JVM system property: -Dlog.path=<path>
        Example: -Dlog.path=C:/BuildMat/logs/buildmat.log
        Default: logs/buildmat.log  (relative to the working directory)
    -->
    <property name="LOG_FILE" value="${log.path:-logs/buildmat.log}"/>

    <!-- ── Console Appender ───────────────────────────────────────────────── -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%logger{30}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── Rolling File Appender ─────────────────────────────────────────── -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover; keeps 30 days of history -->
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%logger{36}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ── Logger Levels ─────────────────────────────────────────────────── -->
    <logger name="com.buildmat" level="DEBUG"/>

    <!-- Suppress noisy third-party INFO logs -->
    <logger name="com.mysql"       level="WARN"/>
    <logger name="org.apache.poi"  level="WARN"/>
    <logger name="com.itextpdf"    level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
